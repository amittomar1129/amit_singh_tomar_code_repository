package designprinciple.core.yagni;

//  YAGNI Principle (You Aren’t Gonna Need It):

//  Don’t build features, abstractions, APIs, layers, or generalizations until they are
//  actually needed — not “possibly needed someday.”
//  Don’t build features or abstractions until they are actually required.
//  It prevents over-engineering, reduces cognitive load, and keeps systems lean and maintainable.

//  ? GOOD (Follows YAGNI)
//  Build the simplest system that supports only Stripe, with a small interface if truly required.
//  When new gateways arrive ? refactor.
//  FAANG interviewers want this mindset.

public class App {

}
